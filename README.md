# PDF2MarkdownApp
PDF-to-Markdown web app built with Django that extracts text and images, preserves image positions with placeholders, provides live preview editing, and exports the final result for further use.
